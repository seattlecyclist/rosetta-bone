# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environments
.venv/
.env
.env.local

# uv
uv.lock.bak

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Project-specific: all derived artifacts go in data/.
# The bare /data entry catches a top-level symlink (used to point a
# worktree at the parent repo's data/ so it can share pillars +
# adapter dir without a copy step); data/ catches the directory itself.
/data
data/

# Hugging Face / mlx caches (default off-tree but be explicit)
.cache/
*.safetensors

# Claude Code internal worktrees
.claude/

# Local Claude Code agent guidance — not for the public repo.
CLAUDE.md
